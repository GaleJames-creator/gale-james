# How to manage webhooks using the Admin-UI

The service provides an Admin UI page for searching, creating, editing, enabling, disabling, or deleting webhooks, and for managing webhook secrets. By registering your URLs, the service generates a data object and sends event notifications, allowing your app to process relevant information.

You can reveal and rotate a webhook's secret in Admin UI.
